name: Build Docker Image
uses: docker/build-push-action@v5
with:
  context:
  file: ./TiendaInspireIdentity/Dockerfile
  push: false
  load: true
  tags: identity-api:test
  cache-from : type=gah
  cache-to: type=gha,mode=max
 


      